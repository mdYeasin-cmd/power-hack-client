import React from 'react';
import Layout from '../Layout/Layout';

const UpdateBillingInfo = () => {
    return (
        <Layout>
            <h2 className="text-2xl font-bold text-center mt-2">Billing Info Update</h2>
            
        </Layout>
    );
};

export default UpdateBillingInfo;